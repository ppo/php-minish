<?php
return [
  "baseMetaTitle" => "phpMinish",

  // "autoloader" => "App::autoloader",
  // "autoloader" => function($class) { include "{$class}.php"; },

  // "metaTitleFormatter" => '%1$s › %2$s',
  // "metaTitleFormatter" => function($default, $title) { return sprintf('%1$s › %2$s', $default, $title); },

  // "routeTitleFormatter" => function($routeName) {
  //   return strtoupper(trim(str_replace("-", "~", $routeName)));
  // },
];
